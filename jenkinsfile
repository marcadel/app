pipeline {
    agent {
        kubernetes {
            yaml """
kind: Pod
metadata:
  name: kaniko-kubectl
  namespace: jenkins
spec:
  serviceAccountName: jenkins
  containers:
  - name: kubectl
    image: abanobmorkos10/aws-kubectl
    command:
      - "/bin/bash"
      - "-c"
      - "sleep 99d"
    tty: true
"""
        }
    }
    stages {
        stage('Check Kubernetes API') {
            steps {
                container('kubectl') {
                    script {
                        sh 'curl -k https://kubernetes.default.svc'
                    }
                }
            }
        }
        stage('deploy to eks') {
            steps {
                container('kubectl') {
                    script {
                        try {
                            sh 'kubectl version'
                        } catch (Exception e) {
                            echo "Error: ${e}"
                            currentBuild.result = 'FAILURE'
                            throw e
                        }
                    }
                }
            }
        }
    }
    post {
        failure {
            echo 'Deployment failed!'
        }
    }
}
